schema: schema.graphql
extensions:
  endpoints:
    Default GraphQL Endpoint:
      url: https://api-server.circlesubi.localhost/graphql
      headers:
        user-agent: JS GraphQL
      introspect: true
